"use strict"; // treat all JS code as newer version

// alert(3+3) // we are usingh nodejs, not browser

console.log(3
    +
    3) // code readability should be high. never ever do that .

    console.log("Tanweer"); // yeh readable tarika hai.

    let name = "Tanweer"
    let age = 18
    let isLoggedIn = false
    let state;

    // number => 2 to power 53 maximum integer
    // bigint
    // string => ""
    // boolean => true/false
    // null => standalone value
    // undefined =>
    // symbol => unique


    // object

    console.log(typeof "Tanweer"); // kisi bhi value ke type ko chck krne ke liye typeof use kya jata h.

    console.log(typeof undefine); // undefined
    console.log(typeof null); // object
